import Vue from 'vue'
import Vuex from 'vuex'
import fs from './fs'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    
  },
  mutations: {
    
  },
  modules: {
    
  },
  plugins: [
    fs()
  ]
})